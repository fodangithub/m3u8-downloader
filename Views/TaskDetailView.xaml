<Window x:Class="M3U8Downloader.Views.TaskDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:M3U8Downloader.Converters"
        xmlns:views="clr-namespace:M3U8Downloader.Views"
        Title="Task Detail"
        Width="800" Height="600"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">

    <Window.Resources>
        <conv:SegmentStatusToColorConverter x:Key="SegStatusColor"/>
        <conv:StatusToColorConverter x:Key="StatusToColor"/>
        <conv:StatusToTextConverter x:Key="StatusToText"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="4" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <views:CustomTitleBar Grid.Row="0" TitleText="{Binding FileName}"/>

            <!-- Header -->
            <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" Padding="16,12"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- URL (selectable) -->
                    <TextBox Grid.Row="0" Text="{Binding SourceUrl, Mode=OneWay}" FontSize="11"
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             Margin="0,4,0,0" IsReadOnly="True" BorderThickness="0"
                             Background="Transparent" Padding="0"
                             TextWrapping="NoWrap"
                             Cursor="IBeam"
                             FontFamily="Consolas, 'Courier New'"/>

                    <!-- Progress -->
                    <Grid Grid.Row="1" Margin="0,12,0,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ProgressBar Grid.Row="0" Value="{Binding Progress}" Height="22"
                                     Background="{DynamicResource HoverBrush}" Foreground="{DynamicResource PrimaryBrush}"/>

                        <DockPanel Grid.Row="1" Margin="0,6,0,0">
                            <TextBlock DockPanel.Dock="Left" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontFamily="Consolas, 'Courier New'">
                                <Run Text="{Binding Progress, StringFormat={}{0:F1}%, Mode=OneWay}" FontWeight="Bold"/>
                                <Run Text="  |  " Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <Run Text="{Binding CompletedSegments, Mode=OneWay}"/>/<Run Text="{Binding TotalSegments, Mode=OneWay}"/>
                                <Run Text=" segments"/>
                                <Run Text="  |  " Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <Run Text="{Binding SizeText, Mode=OneWay}"/>
                            </TextBlock>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                        DockPanel.Dock="Right">
                                <TextBlock Text="{Binding DownloadSpeedText, Mode=OneWay}"
                                           FontSize="13" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,12,0"
                                           FontWeight="Bold" FontFamily="Consolas, 'Courier New'"/>
                                <TextBlock Text="{Binding RemainingTimeText, Mode=OneWay}"
                                           FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}"
                                           FontFamily="Consolas, 'Courier New'"/>
                            </StackPanel>
                        </DockPanel>
                    </Grid>

                    <!-- Status + Buttons -->
                    <DockPanel Grid.Row="2" VerticalAlignment="Center">
                        <Border Background="{Binding Status, Converter={StaticResource StatusToColor}}"
                                CornerRadius="2" Padding="8,2" DockPanel.Dock="Left" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Status, Converter={StaticResource StatusToText}}"
                                       Foreground="{DynamicResource BackgroundBrush}" FontSize="11" FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       FontFamily="Consolas, 'Courier New'"/>
                        </Border>
                        <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                                   Margin="12,0,0,0" VerticalAlignment="Center"
                                   FontFamily="Consolas, 'Courier New'"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                    DockPanel.Dock="Right" VerticalAlignment="Center">
                            <Button Content="{Binding PauseResumeButtonText}"
                                    Command="{Binding PauseResumeCommand}"
                                    Padding="16,6" Margin="0,0,8,0"
                                    Background="{DynamicResource WarningButtonBg}" Foreground="{DynamicResource WarningBrush}" BorderBrush="{DynamicResource WarningBrush}"
                                    BorderThickness="1" Cursor="Hand" FontSize="13"/>
                            <Button Content="CANCEL" Command="{Binding CancelCommand}"
                                    Padding="16,6" Margin="0,0,8,0"
                                    Background="{DynamicResource ErrorButtonBg}" Foreground="{DynamicResource ErrorBrush}" BorderBrush="{DynamicResource ErrorBrush}"
                                    BorderThickness="1" Cursor="Hand" FontSize="13"/>
                            <Button Content="RETRY" Command="{Binding RetryCommand}"
                                    Padding="16,6"
                                    Background="{DynamicResource PrimaryButtonBg}" Foreground="{DynamicResource PrimaryBrush}" BorderBrush="{DynamicResource PrimaryBrush}"
                                    BorderThickness="1" Cursor="Hand" FontSize="13"
                                    Visibility="{Binding CanRetry, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ErrorMessage}" FontSize="12" Foreground="{DynamicResource ErrorBrush}"
                                   Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                   DockPanel.Dock="Right" TextTrimming="CharacterEllipsis"
                                   FontFamily="Consolas, 'Courier New'"/>
                    </DockPanel>
                </Grid>
            </Border>

            <!-- Segment Grid -->
            <Grid Grid.Row="2" Margin="16,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="// SEGMENTS (click any block to re-download)" FontSize="14" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,8"
                           FontFamily="Consolas, 'Courier New'"/>

                <!-- Legend -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
                    <Border Width="12" Height="12" Background="{DynamicResource SuccessBrush}" CornerRadius="1" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Completed" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,0" VerticalAlignment="Center"
                               FontFamily="Consolas, 'Courier New'"/>
                    <Border Width="12" Height="12" Background="{DynamicResource PrimaryBrush}" CornerRadius="1" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Downloading" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,0" VerticalAlignment="Center"
                               FontFamily="Consolas, 'Courier New'"/>
                    <Border Width="12" Height="12" Background="{DynamicResource WarningBrush}" CornerRadius="1" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Retrying" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,0" VerticalAlignment="Center"
                               FontFamily="Consolas, 'Courier New'"/>
                    <Border Width="12" Height="12" Background="{DynamicResource ErrorBrush}" CornerRadius="1" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Failed" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,0" VerticalAlignment="Center"
                               FontFamily="Consolas, 'Courier New'"/>
                    <Border Width="12" Height="12" Background="{DynamicResource HoverBrush}" CornerRadius="1" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Pending" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"
                               FontFamily="Consolas, 'Courier New'"/>
                    <Button Content="RETRY_ALL_FAILED" Command="{Binding RetryAllCommand}"
                            Padding="10,3" Margin="16,0,0,0" FontSize="11"
                            Background="{DynamicResource PrimaryButtonBg}" Foreground="{DynamicResource PrimaryBrush}"
                            BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1" Cursor="Hand" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Segment Visualization (wrapped grid) -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Segments}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="8" Height="8" Margin="1"
                                        Background="{Binding Status, Converter={StaticResource SegStatusColor}}"
                                        CornerRadius="0" Cursor="Hand"
                                        ToolTip="{Binding Index, StringFormat=Segment #{0} (click to re-download)}">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.RetrySegmentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      CommandParameter="{Binding Index}"/>
                                    </Border.InputBindings>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>
